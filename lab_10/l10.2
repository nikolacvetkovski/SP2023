#include <iostream>
#include <cmath>
using namespace std;

int sumNumber(int n) {   // 12930 ---> 0 + 3 + 0 + 2 + 1 ---> 6
    if(n == 0) {
        return 0;
    }
    
    if(n % 10 >= 5) {
        return 0 + sumNumber(n / 10);
    }
    
    return n % 10 + sumNumber(n / 10);
}

int main()
{
    int number, n, result, maxSum = 0, maxNumber;
    cin >> n;
    
    for(int i = 0; i < n; i++) {
        cin >> number;
        result = sumNumber(number);
        
        if(result > maxSum) {
            maxSum = result;
            maxNumber = number;
        }
    }
    
    cout << maxNumber << endl;
    return 0;
}
