#include <iostream>
using namespace std;

int main()
{
    int m[100][100], n, sum1 = 0, sum2 = 0;
    bool isSquare = false;
    cin >> n;
    
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            cin >> m[i][j];
        }
    }
    
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            if(i == j) {
                sum1 += m[i][j];
            }
            if(i + j == n - 1) {
                sum2 += m[i][j];
            }
        }
    }
    
    for(int i = 1; i < sum1; i++) {
        if(i * i == sum1) {
            isSquare = true;
        }
    }
    for(int i = 1; i < sum2; i++) {
        if(i * i == sum2) {
            isSquare = true;
        }
    }
    
    !isSquare ? cout << "NE" : cout << "DA";
    return 0;
}
