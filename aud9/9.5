#include <iostream>
using namespace std;

int sumDigits(int number) {
    if(number == 0) {
        return 0;
    }
    
    return number % 10 + sumDigits(number / 10);
}

int main() 
{
    int n;
    cin >> n;
    
    cout << sumDigits(n);

    return 0;
}
