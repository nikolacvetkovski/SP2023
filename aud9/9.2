#include <iostream>
using namespace std;

void count_down(int n) {
    if(n == 0) {
        cout << "0" << endl;
        return;
    }
    
    cout << n << " ";
    count_down(n - 1);
}

int main()
{
    int n;
    cin >> n;
    
    count_down(n);

    return 0;
}
