#include <iostream>
using namespace std;

int main()
{
    char a;
    cin >> a;

    int result = (a >= 'a') && (a <= 'z');
    /*int result = (a >= '0') && (a <= '9');*/
    cout << result;
    return 0;
}
