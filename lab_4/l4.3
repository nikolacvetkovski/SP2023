#include <iostream>
using namespace std;

int main() {
    int a, b, counter = 0, printedNumbers = 0;
    bool isTrue = false;
    cin >> a >> b;   // 1234567   654321
    
    for(int i=a+1; i<=b; i++) {
        ++counter;
        if((i % 2 == 0) && (i % 7 == 0) || (i % 2 != 0) && (i % 3 != 0)) {
            isTrue = true;
            ++printedNumbers;
            cout << i << endl;
        }
        else {
            isTrue = false;
        }
    }
    
    if(!isTrue) {
        cout << "0" << endl;
    }
    else {
        cout << (float) printedNumbers / counter * 100 << "%" << endl;
    }

    return 0;
}
