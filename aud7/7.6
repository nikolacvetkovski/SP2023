#include <iostream>
using namespace std;

int main() {
  int array[100], n, m;
  cin >> n;
  
  for(int i = 0; i < n; i++) {
      cin >> array[i];
  }
  
  cin >> m;
  for(int j = 0; j < m; j++) {
      int temp = array[n - 1];
      for(int i = n - 1; i >= 0; i--) {
          array[i] = array[i - 1];
      }
      array[0] = temp;
  }
  
  for(int i = 0; i < n; i++) {
      cout << array[i] << " ";
  }
  return 0;
}
