#include <iostream>
using namespace std;

int main() {
  int array[100], n, counter = 0;
  cin >> n;
  
  for(int i = 0; i < n; i++) {
      cin >> array[i];
  }
  
  for(int i = 0; i < n - counter; i++) {
      for(int j = i + 1; j < n - counter; j++) {
          if(array[i] == array[j]) {
              for(int k = j; k < n - 1 - counter; k++) {
                  array[k] = array[k + 1];
              }
               ++counter;
               --j;
          }
      }
  }
  
  n -= counter;
  for(int i = 0; i < n; i++) {
      cout << array[i] << " ";
  }
  
  return 0;
}
