#include <iostream>
using namespace std;

int par(int a[], int n) {
    int countOccur, occurences[n], minElement = 999999999;
    bool isTrue = false;
    for(int i = 0; i < n; i++) {
        countOccur = 0;
        for(int j = 0; j < n; j++) {
            if(a[i] == a[j]) {
                ++countOccur;
            }
        }
        occurences[i] = countOccur;
    }
    
    for(int i = 0; i < n; i++) {
        if((a[i] < minElement) && (occurences[i] % 2 == 0)) {
            isTrue = true;
            minElement = a[i];
        }
    }
    
    if(isTrue) {
        return minElement;
    }
    else {
        return 0;
    }
}

int main() {
    int a[100], n;
    cin >> n;
    
    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }
    
    int result = par(a, n);
    
    if(result > 0) {
        cout << "Najmaliot element koj se pojavuva paren broj pati e " << result << endl;
    }
    else {
        cout << "Nitu eden element ne se pojavuva paren broj pati!" << endl;
    }
    
    return 0;
}
