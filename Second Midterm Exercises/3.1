#include <iostream>
#include <cstring>
using namespace std;

int main()
{
    char line[50];
    int n, counter;
    
    cin >> n;
    cin.ignore(100, '\n');
    
    for(int i = 0; i < n; i++) {
        cin.getline(line, 50);
        int size = strlen(line);
        
        for(int j = 0; j < size; j++) {
            line[j] = tolower(line[j]);
        }
        
        counter = 0;
        for(int j = 0; j < size; j++) {
            if(strncmp("a1c", line + j, 3) == 0) {
                ++counter;
            }
        }
        
        if(counter >= 2) {
           cout << line << endl; 
        }
    }
    
    return 0;
}
