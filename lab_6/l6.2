#include <iostream>
using namespace std;

int main()
{
   int array[100], n;
   bool isPrime = true;
   cin >> n;
   
   for(int i = 0; i < n; i++) {
       cin >> array[i];
   }
   
   for(int i = 0; i < n; i+=2) {
       isPrime = true;
       if(n % 2 == 0) {
           int twoDigit = array[i] * 10 + array[i + 1];
           for(int j = 2; j < twoDigit; j++) {
               if(twoDigit % j == 0) {
                   isPrime = false;
                   break;
               }
           }
           if(isPrime) {
               cout << "brojot " << twoDigit << " e prost" << endl;
           }
           else {
               cout << "brojot " << twoDigit << " NE e prost" << endl;
           }
       }
       else {
           if(i == n - 1) {
               int twoDigit = array[i];
               for(int j = 2; j < twoDigit; j++) {
                   if(twoDigit % j == 0) {
                       isPrime = false;
                       break;
                   }
               }
               if(isPrime) {
                   cout << "brojot " << twoDigit << " e prost" << endl;
               }
               else {
                   cout << "brojot " << twoDigit << " NE e prost" << endl;
               }
           }
           else {
               int twoDigit = array[i] * 10 + array[i + 1];
               for(int j = 2; j < twoDigit; j++) {
                   if(twoDigit % j == 0) {
                       isPrime = false;
                       break;
                   }
               }
               if(isPrime) {
                   cout << "brojot " << twoDigit << " e prost" << endl;
               }
               else {
                   cout << "brojot " << twoDigit << " NE e prost" << endl;
               }
           }
       }
   }
   
   return 0;
}
