#include <iostream>
using namespace std;

bool isPrime(int number) {
    for(int i = 2; i < number; i++) {
        if(number % i == 0) {
            return false;
        }
    }
    return true;
}

int twoDigitTransform(int number1, int number2) {
    return number1 * 10 + number2;
}

int main()
{
   int array[100], n;
   cin >> n;
   
   for(int i = 0; i < n; i++) {
       cin >> array[i];
   }
   
   for(int i = 0; i < n; i+=2) {
      if(n % 2 == 1 && i == n - 1) {
          if(isPrime(array[i])) {
              cout << "brojot " << array[i] << " e prost" << endl;
          }
          else {
              cout << "brojot " << array[i] << " NE e prost" << endl;
          }
      }
      else {
          if(isPrime(twoDigitTransform(array[i], array[i + 1]))) {
              cout << "brojot " << twoDigitTransform(array[i], array[i + 1]) << " e prost" << endl;
          }
          else {
              cout << "brojot " << twoDigitTransform(array[i], array[i + 1]) << " NE e prost" << endl;
          }
      }
   }
   
   return 0;
}
