#include <iostream>
using namespace std;

int main() {

    int num1, num2, num3, num4;
    cin >> num1 >> num2 >> num3 >> num4;

    if((num1 * num2) % 2 == 0 && (num3 * num4) % 2 == 1) {
        cout << "1";
    }
    else {
        cout << "0";
    }
    return 0;
}
